## Setup
For the bro tomus

```sh
# clone the repo
gh repo clone PasNinii/turf-debug-test

# install npm dep
npm install

# run locally
npm run start
# or
ng serve # this require angular sudo npm i -g @angular/cli
```
